<UserControl x:Class="CSVC_PTIT.App.Views.QL.DanhSachDonDaDuyetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="600">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadDataCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Danh sách đơn mượn"
                   Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                   Margin="0,0,0,16" />

        <Button Grid.Row="1"
                Command="{Binding LoadDataCommand}"
                Style="{StaticResource MaterialDesignFlatButton}"
                HorizontalAlignment="Left"
                Margin="0,0,0,12"
                ToolTip="Làm mới danh sách">
            <StackPanel Orientation="Horizontal">
                <md:PackIcon Kind="Refresh" Margin="0,0,4,0" />
                <TextBlock Text="Làm mới" />
            </StackPanel>
        </Button>

        <md:Card Grid.Row="2" md:ElevationAssist.Elevation="Dp2">
            <Grid>
                <ProgressBar Style="{StaticResource MaterialDesignLinearProgressBar}"
                             IsIndeterminate="True"
                             Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                             VerticalAlignment="Top" />

                <DataGrid ItemsSource="{Binding ApprovedRequests}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserSortColumns="True"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          Style="{StaticResource MaterialDesignDataGrid}"
                          Margin="0,4,0,0">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã Đơn" Binding="{Binding RequestCode}" Width="100" />
                        <DataGridTextColumn Header="Người Mượn" Binding="{Binding Requester.FullName}" Width="150" />
                        <DataGridTextColumn Header="Bắt Đầu" Binding="{Binding BorrowStartAt, StringFormat=\{0:dd/MM/yyyy HH:mm\}}" Width="140" />
                        <DataGridTextColumn Header="Kết Thúc" Binding="{Binding BorrowEndAt, StringFormat=\{0:dd/MM/yyyy HH:mm\}}" Width="140" />
                        <DataGridTextColumn Header="Trạng Thái" Binding="{Binding Status}" Width="110" />
                        
                        <DataGridTemplateColumn Header="Thao tác" Width="200">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="KT Kho"
                                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                                Command="{Binding DataContext.CheckAssetsCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                Margin="0,0,8,0"
                                                Height="28" FontSize="11" />
                                        <Button Content="Bàn Giao"
                                                Style="{StaticResource MaterialDesignFlatButton}"
                                                CommandParameter="{Binding}"
                                                Height="28" FontSize="11" />
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </md:Card>

    </Grid>
</UserControl>
