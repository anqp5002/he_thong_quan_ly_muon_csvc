<UserControl x:Class="CSVC_PTIT.App.Views.QL.BanGiaoCSVCView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="{DynamicResource MaterialDesignPaper}">

    <Grid Margin="20">
        <TabControl materialDesign:ColorZoneAssist.Mode="PrimaryMid" Style="{StaticResource MaterialDesignFilledTabControl}">
            <!-- TAB 1: Danh sách đơn đã duyệt (C.1) -->
            <TabItem Header="1. Danh sách đơn chờ bàn giao">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Margin="0,10,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Danh Sách Đơn Mượn Đã Được Duyệt" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" />
                        <TextBox x:Name="TxtSearch" Grid.Column="1" Width="250" materialDesign:HintAssist.Hint="Tìm theo Mã SV hoặc Mã đơn..." TextChanged="TxtSearch_TextChanged" Style="{StaticResource MaterialDesignOutlinedTextBox}" Padding="8"/>
                    </Grid>
                    
                    <DataGrid x:Name="GridDanhSachDon" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" GridLinesVisibility="All" SelectionChanged="GridDanhSachDon_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Mã Đơn" Binding="{Binding RequestCode}" Width="120"/>
                            <DataGridTextColumn Header="Người Mượn" Binding="{Binding Requester.FullName}" Width="200"/>
                            <DataGridTextColumn Header="Bắt đầu" Binding="{Binding BorrowStartAt, StringFormat=\{0:dd/MM/yyyy HH:mm\}}" Width="150"/>
                            <DataGridTextColumn Header="Dự kiến trả" Binding="{Binding ExpectedReturnAt, StringFormat=\{0:dd/MM/yyyy HH:mm\}}" Width="150"/>
                            <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- TAB 2: Kiểm tra tồn kho (C.2) -->
            <TabItem Header="2. Kiểm tra vật tư khả dụng" x:Name="TabKiemTra">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Đối chiếu Yêu cầu và Tồn kho" FontSize="20" FontWeight="Bold" Margin="0,10,0,20"/>

                    <DataGrid x:Name="GridKiemTra" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" GridLinesVisibility="All">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Tên CSVC" Binding="{Binding Asset.AssetName}" Width="250"/>
                            <DataGridTextColumn Header="SL Yêu Cầu" Binding="{Binding QuantityApproved}" Width="120"/>
                            <DataGridTextColumn Header="Tồn Kho Khả Dụng" Binding="{Binding Asset.AvailableQuantity}" Width="150"/>
                            <DataGridTextColumn Header="Tình trạng CSVC" Binding="{Binding Asset.ConditionStatus}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                        <Button x:Name="BtnKiemTraHopLe" Content="Xác nhận &amp; Chuyển sang Bàn Giao" Click="BtnKiemTraHopLe_Click"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- TAB 3: Phiếu bàn giao (C.4) -->
            <TabItem Header="3. Tạo phiếu bàn giao" x:Name="TabPhieuBanGiao">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Biểu mẫu Bàn Giao (QL_BM03)" FontSize="20" FontWeight="Bold" Margin="0,10,0,20"/>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
                        <TextBox x:Name="TxtGhiChuBanGiao" materialDesign:HintAssist.Hint="Ghi chú bàn giao (Tùy chọn)" Width="400" Margin="0,0,20,0"/>
                        <TextBlock x:Name="TxtNguoiNhan" Text="Người nhận: " VerticalAlignment="Center" FontWeight="SemiBold" FontSize="14"/>
                    </StackPanel>

                    <DataGrid x:Name="GridChiTietBanGiao" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False" GridLinesVisibility="All" SelectionUnit="Cell">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="CSVC" Binding="{Binding Asset.AssetName}" Width="200" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Số lượng giao" Binding="{Binding QuantityApproved}" Width="120" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Ghi nhận tình trạng (Tốt/Xước/Hỏng)" Binding="{Binding ItemNote}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                        <Button x:Name="BtnHoanTatBanGiao" Content="Hoàn Tất Bàn Giao" Click="BtnHoanTatBanGiao_Click" Margin="0,0,10,0"/>
                        <Button x:Name="BtnInPhieu" Content="In Phiếu PDF" Style="{StaticResource MaterialDesignOutlinedButton}" Click="BtnInPhieu_Click" IsEnabled="False"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- TAB 4: Nhận trả CSVC -->
            <TabItem Header="4. Nhận trả CSVC" x:Name="TabNhanTra">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Nhận Trả CSVC" FontSize="20" FontWeight="Bold" Margin="0,10,0,10"/>
                    <TextBlock x:Name="TxtOverdueWarning" Grid.Row="0" Text="" FontSize="13" Foreground="Red" FontWeight="Bold" Margin="0,36,0,0"/>

                    <!-- Danh sách đơn đã bàn giao -->
                    <StackPanel Grid.Row="1" Margin="0,10,0,10">
                        <TextBlock Text="Chọn đơn đã bàn giao cần nhận trả:" FontSize="14" Margin="0,0,0,8"/>
                        <DataGrid x:Name="GridCheckedOut" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" 
                                  GridLinesVisibility="All" SelectionChanged="GridCheckedOut_SelectionChanged"
                                  MaxHeight="180">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Mã Đơn" Binding="{Binding BorrowRequest.RequestCode}" Width="100"/>
                                <DataGridTextColumn Header="Mã BG" Binding="{Binding CheckoutCode}" Width="120"/>
                                <DataGridTextColumn Header="Người Mượn" Binding="{Binding CheckedOutToUser.FullName}" Width="160"/>
                                <DataGridTextColumn Header="Hạn Trả" Binding="{Binding BorrowRequest.ExpectedReturnAt, StringFormat=\{0:dd/MM HH:mm\}}" Width="120"/>
                                <DataGridTextColumn Header="Trạng Thái" Binding="{Binding OverdueStatus}" Width="*"/>
                            </DataGrid.Columns>
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsOverdue1Hour}" Value="True">
                                            <Setter Property="Background" Value="#FFCDD2"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsOverdueNormal}" Value="True">
                                            <Setter Property="Background" Value="#FFF9C4"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                        </DataGrid>
                    </StackPanel>

                    <!-- Chi tiết CSVC cần trả -->
                    <DataGrid x:Name="GridChiTietTra" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False" GridLinesVisibility="All" SelectionUnit="Cell">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="CSVC" Binding="{Binding Asset.AssetName}" Width="200" IsReadOnly="True"/>
                            <DataGridTextColumn Header="SL Đã Giao" Binding="{Binding Quantity}" Width="120" IsReadOnly="True"/>
                            <DataGridTextColumn Header="SL Trả" Binding="{Binding QuantityReturned}" Width="100"/>
                            <DataGridTemplateColumn Header="Tình trạng" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox SelectedValue="{Binding ConditionAfter, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Content">
                                            <ComboBoxItem Content="Tốt" IsSelected="True"/>
                                            <ComboBoxItem Content="Hỏng"/>
                                            <ComboBoxItem Content="Mất"/>
                                        </ComboBox>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Ghi chú hư hỏng" Binding="{Binding DamageNote}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
                        <TextBox x:Name="TxtGhiChuTra" materialDesign:HintAssist.Hint="Ghi chú khi trả (tùy chọn)" Width="400"/>
                    </StackPanel>

                    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button x:Name="BtnHoanTatTra" Content="Hoàn Tất Nhận Trả" Click="BtnHoanTatTra_Click" Margin="0,0,10,0" IsEnabled="False"/>
                        <Button x:Name="BtnInPhieuTra" Content="In Phiếu Trả PDF" Style="{StaticResource MaterialDesignOutlinedButton}" Click="BtnInPhieuTra_Click" IsEnabled="False"/>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>

