<UserControl x:Class="CSVC_PTIT.App.Views.QL.DoiSoatCSVCView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="{DynamicResource MaterialDesignPaper}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Nhận Trả &amp; Đối Soát CSVC (Sprint 2)" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <ComboBox x:Name="CbbPhieuBanGiao" materialDesign:HintAssist.Hint="Chọn Phiếu Bàn Giao Cần Trả" Width="300" Margin="0,0,20,0" SelectionChanged="CbbPhieuBanGiao_SelectionChanged">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding CheckoutCode}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <TextBox x:Name="TxtGhiChuTra" materialDesign:HintAssist.Hint="Ghi chú khi trả" Width="300" Margin="0,0,20,0"/>
        </StackPanel>

        <DataGrid x:Name="GridChiTietTra" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False" CanUserReorderColumns="False" GridLinesVisibility="All" SelectionUnit="Cell">
            <DataGrid.Columns>
                <DataGridTextColumn Header="CSVC" Binding="{Binding Asset.AssetName}" Width="200" IsReadOnly="True"/>
                <DataGridTextColumn Header="Số lượng đã giao" Binding="{Binding Quantity}" Width="150" IsReadOnly="True"/>
                
                <!-- Cột nhập số lượng trả thực tế -->
                <DataGridTextColumn Header="Số lượng trả" Binding="{Binding QuantityReturned}" Width="120"/>
                
                <!-- Cột tình trạng sau khi trả -->
                <DataGridTemplateColumn Header="Tình trạng (Tốt/Hỏng/Mất)" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox SelectedValue="{Binding ConditionAfter, UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Content">
                                <ComboBoxItem Content="Tốt" IsSelected="True"/>
                                <ComboBoxItem Content="Hỏng"/>
                                <ComboBoxItem Content="Mất"/>
                            </ComboBox>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Ghi chú hư hỏng" Binding="{Binding DamageNote}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="BtnHoanTatTra" Content="Hoàn Tất Nhận Trả" Click="BtnHoanTatTra_Click" Margin="0,0,10,0"/>
            <Button x:Name="BtnInPhieuTra" Content="In Phiếu PDF" Style="{StaticResource MaterialDesignOutlinedButton}" Click="BtnInPhieuTra_Click" IsEnabled="False"/>
        </StackPanel>
    </Grid>
</UserControl>
