<UserControl x:Class="CSVC_PTIT.App.Views.QuanLyTaiKhoanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar: Tìm kiếm + Nút Thêm -->
        <materialDesign:Card Grid.Row="0" Margin="0,0,0,16" Padding="16" UniformCornerRadius="8">
            <DockPanel>
                <Button DockPanel.Dock="Right"
                        Content="THÊM TÀI KHOẢN"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Click="BtnAdd_Click"
                        Margin="16,0,0,0">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="AccountPlus" VerticalAlignment="Center"/>
                                <TextBlock Text=" THÊM TÀI KHOẢN" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>

                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="Tìm theo tên, email..."
                         materialDesign:TextFieldAssist.HasLeadingIcon="True"
                         materialDesign:TextFieldAssist.LeadingIcon="Magnify"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         VerticalAlignment="Center"/>
            </DockPanel>
        </materialDesign:Card>

        <!-- Loading spinner -->
        <ProgressBar Grid.Row="1"
                     Style="{StaticResource MaterialDesignCircularProgressBar}"
                     IsIndeterminate="True"
                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                     VerticalAlignment="Top" Margin="0,16,0,0"/>

        <!-- DataGrid: Danh sách tài khoản -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Users}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  materialDesign:DataGridAssist.CornerRadius="8"
                  materialDesign:DataGridAssist.CellPadding="13 8 8 8"
                  HeadersVisibility="Column">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã TK" Binding="{Binding UserId}" Width="60"/>
                <DataGridTextColumn Header="Họ tên" Binding="{Binding FullName}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Vai trò" Binding="{Binding Role.RoleName}" Width="120"/>

                <!-- Trạng thái có màu -->
                <DataGridTemplateColumn Header="Trạng thái" Width="110">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <materialDesign:Chip>
                                <materialDesign:Chip.Style>
                                    <Style TargetType="materialDesign:Chip" BasedOn="{StaticResource {x:Type materialDesign:Chip}}">
                                        <Setter Property="Content" Value="Hoạt động"/>
                                        <Setter Property="Background" Value="#E8F5E9"/>
                                        <Setter Property="Foreground" Value="#2E7D32"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Status}" Value="Locked">
                                                <Setter Property="Content" Value="Bị khóa"/>
                                                <Setter Property="Background" Value="#FFEBEE"/>
                                                <Setter Property="Foreground" Value="#C62828"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Inactive">
                                                <Setter Property="Content" Value="Ngưng"/>
                                                <Setter Property="Background" Value="#FFF3E0"/>
                                                <Setter Property="Foreground" Value="#E65100"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </materialDesign:Chip.Style>
                            </materialDesign:Chip>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Nút hành động -->
                <DataGridTemplateColumn Header="Hành động" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Style="{StaticResource MaterialDesignIconButton}"
                                        ToolTip="Sửa" Click="BtnEdit_Click" Tag="{Binding}">
                                    <materialDesign:PackIcon Kind="Pencil" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                                </Button>
                                <Button Style="{StaticResource MaterialDesignIconButton}"
                                        ToolTip="Khóa/Mở khóa" Click="BtnToggleLock_Click" Tag="{Binding}">
                                    <materialDesign:PackIcon Kind="Lock" Foreground="#FF9800"/>
                                </Button>
                                <Button Style="{StaticResource MaterialDesignIconButton}"
                                        ToolTip="Xóa" Click="BtnDelete_Click" Tag="{Binding}">
                                    <materialDesign:PackIcon Kind="Delete" Foreground="#F44336"/>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
