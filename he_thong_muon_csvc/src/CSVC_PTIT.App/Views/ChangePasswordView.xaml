<Window x:Class="CSVC_PTIT.App.Views.ChangePasswordView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Đổi mật khẩu" Height="400" Width="350"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}">
    <Grid Margin="16">
        <StackPanel>
            <TextBlock Text="ĐỔI MẬT KHẨU" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,16" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
            
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" Margin="0,0,0,8" HorizontalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ErrorMessage}" Value="">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <PasswordBox x:Name="txtOldPassword"
                         materialDesign:HintAssist.Hint="Mật khẩu cũ" 
                         Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" 
                         Margin="0,0,0,16" 
                         PasswordChanged="txtOldPassword_PasswordChanged"/>

            <PasswordBox x:Name="txtNewPassword"
                         materialDesign:HintAssist.Hint="Mật khẩu mới (ít nhất 6 ký tự)" 
                         Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" 
                         Margin="0,0,0,16" 
                         PasswordChanged="txtNewPassword_PasswordChanged"/>
                         
            <PasswordBox x:Name="txtConfirmPassword"
                         materialDesign:HintAssist.Hint="Xác nhận mật khẩu mới" 
                         Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" 
                         Margin="0,0,0,24" 
                         PasswordChanged="txtConfirmPassword_PasswordChanged"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="HỦY" Style="{StaticResource MaterialDesignOutlinedButton}" Click="BtnCancel_Click"/>
                <Button Grid.Column="2" Content="LƯU" Command="{Binding ChangePasswordCommand}" Style="{StaticResource MaterialDesignRaisedButton}"/>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
