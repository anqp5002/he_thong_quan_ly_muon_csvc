<UserControl x:Class="CSVC_PTIT.App.Views.DT.DanhSachDonCanDuyetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="600">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadDataCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <TextBlock Grid.Row="0"
                   Text="Danh sách đơn mượn chờ duyệt"
                   Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                   Margin="0,0,0,16" />

        <!-- Nút Refresh -->
        <Button Grid.Row="1"
                Command="{Binding LoadDataCommand}"
                Style="{StaticResource MaterialDesignFlatButton}"
                HorizontalAlignment="Left"
                Margin="0,0,0,12"
                ToolTip="Làm mới danh sách">
            <StackPanel Orientation="Horizontal">
                <md:PackIcon Kind="Refresh" Margin="0,0,4,0" />
                <TextBlock Text="Làm mới" />
            </StackPanel>
        </Button>

        <!-- DataGrid kết quả -->
        <md:Card Grid.Row="2" md:ElevationAssist.Elevation="Dp2">
            <Grid>
                <ProgressBar Style="{StaticResource MaterialDesignLinearProgressBar}"
                             IsIndeterminate="True"
                             Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                             VerticalAlignment="Top" />

                <DataGrid ItemsSource="{Binding PendingRequests}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserSortColumns="True"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          Style="{StaticResource MaterialDesignDataGrid}"
                          Margin="0,4,0,0">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã Đơn" Binding="{Binding RequestCode}" Width="100" />
                        <DataGridTextColumn Header="Ngày Tạo" Binding="{Binding CreatedAt, StringFormat=\{0:dd/MM/yyyy HH:mm\}}" Width="140" />
                        <DataGridTextColumn Header="Tiêu Đề" Binding="{Binding Title}" Width="*" />
                        <DataGridTextColumn Header="Bắt Đầu" Binding="{Binding BorrowStartAt, StringFormat=\{0:dd/MM/yyyy HH:mm\}}" Width="140" />
                        <DataGridTextColumn Header="Kết Thúc" Binding="{Binding BorrowEndAt, StringFormat=\{0:dd/MM/yyyy HH:mm\}}" Width="140" />
                        <DataGridTemplateColumn Header="Thao tác" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Duyệt"
                                                Style="{StaticResource MaterialDesignFlatButton}"
                                                Command="{Binding DataContext.ApproveRequestCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                Margin="0,0,8,0"
                                                Height="28" FontSize="11" />
                                        <Button Content="Từ chối"
                                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                                Foreground="OrangeRed"
                                                BorderBrush="OrangeRed"
                                                Command="{Binding DataContext.RejectRequestCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                Height="28" FontSize="11" />
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </md:Card>

    </Grid>
</UserControl>
