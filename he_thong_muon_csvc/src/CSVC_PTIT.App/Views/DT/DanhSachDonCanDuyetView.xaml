<Window x:Class="CSVC_PTIT.App.Views.DT.DanhSachDonCanDuyetView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Danh Sách Đơn Chờ Duyệt" Height="600" Width="900">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <TextBlock Grid.Row="0" Text="Danh Sách Đơn Chờ Duyệt"
                   Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                   Margin="0,0,0,16"/>

        <!-- Nút tải -->
        <Button Grid.Row="1" Content="Tải danh sách"
                Command="{Binding TaiDanhSachCommand}"
                Style="{StaticResource MaterialDesignRaisedButton}"
                HorizontalAlignment="Left"
                Margin="0,0,0,16"/>

        <!-- Bảng danh sách -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding DanhSachDon}"
                  SelectedItem="{Binding DonDuocChon}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  materialDesign:DataGridAssist.CellPadding="8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã đơn" Binding="{Binding RequestCode}" Width="100"/>
                <DataGridTextColumn Header="Tên sự kiện" Binding="{Binding Title}" Width="200"/>
                <DataGridTextColumn Header="Loại đơn" Binding="{Binding RequestType}" Width="120"/>
                <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="120"/>
                <DataGridTextColumn Header="Ngày tạo" Binding="{Binding CreatedAt, StringFormat=dd/MM/yyyy HH:mm}" Width="150"/>
                <DataGridTextColumn Header="Bắt đầu" Binding="{Binding BorrowStartAt, StringFormat=dd/MM/yyyy HH:mm}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>