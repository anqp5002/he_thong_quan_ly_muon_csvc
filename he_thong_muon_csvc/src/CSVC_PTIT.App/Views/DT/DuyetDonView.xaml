<Window x:Class="CSVC_PTIT.App.Views.DT.DuyetDonView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Duyệt Đơn Mượn" Height="600" Width="800">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <TextBlock Grid.Row="0" Text="Chi Tiết Đơn Mượn"
                   Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                   Margin="0,0,0,16"/>

        <!-- Thông tin đơn -->
        <GroupBox Grid.Row="1" Header="Thông tin đơn"
                  Style="{StaticResource MaterialDesignGroupBox}"
                  Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Mã đơn:" Margin="8" FontWeight="Bold"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding DonHienTai.RequestCode}" Margin="8"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Tên sự kiện:" Margin="8" FontWeight="Bold"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding DonHienTai.Title}" Margin="8"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Mục đích:" Margin="8" FontWeight="Bold"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding DonHienTai.Purpose}" Margin="8"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Thời gian:" Margin="8" FontWeight="Bold"/>
                <TextBlock Grid.Row="3" Grid.Column="1" Margin="8">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}{0:dd/MM/yyyy HH:mm} → {1:dd/MM/yyyy HH:mm}">
                            <Binding Path="DonHienTai.BorrowStartAt"/>
                            <Binding Path="DonHienTai.BorrowEndAt"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Ghi chú:" Margin="8" FontWeight="Bold"/>
                <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding DonHienTai.RequestNote}" Margin="8"/>
            </Grid>
        </GroupBox>

        <!-- Danh sách CSVC -->
        <GroupBox Grid.Row="2" Header="Danh sách CSVC yêu cầu"
                  Style="{StaticResource MaterialDesignGroupBox}"
                  Margin="0,0,0,16">
            <DataGrid ItemsSource="{Binding DonHienTai.BorrowRequestAssets}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      materialDesign:DataGridAssist.CellPadding="8">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã CSVC" Binding="{Binding AssetId}" Width="100"/>
                    <DataGridTextColumn Header="Số lượng yêu cầu" Binding="{Binding QuantityRequested}" Width="150"/>
                    <DataGridTextColumn Header="Ghi chú" Binding="{Binding ItemNote}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- Lý do từ chối -->
        <TextBox Grid.Row="3"
                 Text="{Binding LyDoTuChoi, UpdateSourceTrigger=PropertyChanged}"
                 materialDesign:HintAssist.Hint="Lý do từ chối (nếu từ chối)"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 AcceptsReturn="True" Height="80"
                 Margin="0,0,0,16"/>

        <!-- Thông báo + Nút -->
        <StackPanel Grid.Row="4">
            <TextBlock Text="{Binding ThongBao}" FontWeight="Bold"
                       Margin="0,0,0,8"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Từ chối"
                        Command="{Binding TuChoiDonCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Foreground="Red"
                        Margin="0,0,8,0"/>

                <Button Content="Duyệt đơn"
                        Command="{Binding DuyetDonCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"/>
            </StackPanel>
        </StackPanel>

    </Grid>
</Window>