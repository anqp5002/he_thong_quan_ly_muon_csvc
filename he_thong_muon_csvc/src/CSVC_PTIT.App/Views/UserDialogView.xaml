<Window x:Class="CSVC_PTIT.App.Views.UserDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Thông tin tài khoản" Height="580" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24">
            <TextBlock x:Name="TxtDialogTitle" Text="THÊM TÀI KHOẢN" FontSize="20" FontWeight="Bold"
                       HorizontalAlignment="Center" Margin="0,0,0,20"
                       Foreground="{DynamicResource PrimaryHueMidBrush}"/>

            <TextBox x:Name="TxtFullName"
                     materialDesign:HintAssist.Hint="Họ tên *"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,12"/>

            <TextBox x:Name="TxtEmail"
                     materialDesign:HintAssist.Hint="Email *"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,12"/>

            <PasswordBox x:Name="TxtPassword"
                         materialDesign:HintAssist.Hint="Mật khẩu (mặc định: 123456)"
                         Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                         Margin="0,0,0,12"/>

            <TextBox x:Name="TxtPhone"
                     materialDesign:HintAssist.Hint="Số điện thoại"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,12"/>

            <TextBox x:Name="TxtStudentCode"
                     materialDesign:HintAssist.Hint="MSSV (nếu là SV)"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,12"/>

            <TextBox x:Name="TxtClassName"
                     materialDesign:HintAssist.Hint="Lớp (nếu là SV)"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,12"/>

            <ComboBox x:Name="CmbRole"
                      materialDesign:HintAssist.Hint="Vai trò *"
                      Style="{StaticResource MaterialDesignOutlinedComboBox}"
                      DisplayMemberPath="RoleName"
                      SelectedValuePath="RoleId"
                      Margin="0,0,0,12"/>

            <ComboBox x:Name="CmbDepartment"
                      materialDesign:HintAssist.Hint="Phòng ban"
                      Style="{StaticResource MaterialDesignOutlinedComboBox}"
                      DisplayMemberPath="DepartmentName"
                      SelectedValuePath="DepartmentId"
                      Margin="0,0,0,12"/>

            <ComboBox x:Name="CmbBorrowerType"
                      materialDesign:HintAssist.Hint="Loại người mượn"
                      Style="{StaticResource MaterialDesignOutlinedComboBox}"
                      Margin="0,0,0,24"/>

            <TextBlock x:Name="TxtError" Foreground="Red" TextWrapping="Wrap"
                       Margin="0,0,0,12" Visibility="Collapsed"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="HỦY"
                        Style="{StaticResource MaterialDesignOutlinedButton}" Click="BtnCancel_Click"/>
                <Button Grid.Column="2" Content="LƯU"
                        Style="{StaticResource MaterialDesignRaisedButton}" Click="BtnSave_Click"/>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</Window>
