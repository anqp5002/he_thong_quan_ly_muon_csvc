<UserControl x:Class="CSVC_PTIT.App.Views.ThongBaoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="600">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadDataCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Thông báo"
                   Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                   Margin="0,0,0,16" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Command="{Binding LoadDataCommand}"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    ToolTip="Làm mới">
                <StackPanel Orientation="Horizontal">
                    <md:PackIcon Kind="Refresh" Margin="0,0,4,0" />
                    <TextBlock Text="Làm mới" />
                </StackPanel>
            </Button>

            <Button Command="{Binding MarkAllAsReadCommand}"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Margin="8,0,0,0"
                    ToolTip="Đánh dấu tất cả đã đọc">
                <StackPanel Orientation="Horizontal">
                    <md:PackIcon Kind="CheckAll" Margin="0,0,4,0" />
                    <TextBlock Text="Đọc tất cả" />
                </StackPanel>
            </Button>
        </StackPanel>

        <md:Card Grid.Row="2" md:ElevationAssist.Elevation="Dp2">
            <Grid>
                <ProgressBar Style="{StaticResource MaterialDesignLinearProgressBar}"
                             IsIndeterminate="True"
                             Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                             VerticalAlignment="Top" />

                <!-- Empty state -->
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                            Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=true}">
                </StackPanel>

                <ListView ItemsSource="{Binding Notifications}" Margin="0,4,0,0">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <md:Card Padding="16" Margin="4,4,4,4" md:ElevationAssist.Elevation="Dp1"
                                     HorizontalAlignment="Stretch">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <md:PackIcon Kind="BellRing" Width="28" Height="28"
                                                 Foreground="{DynamicResource PrimaryHueMidBrush}"
                                                 VerticalAlignment="Center"
                                                 Margin="0,0,12,0" />

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Title}"
                                                   FontWeight="SemiBold" FontSize="14"
                                                   TextWrapping="Wrap" />
                                        <TextBlock Text="{Binding Message}"
                                                   FontSize="12" Foreground="Gray"
                                                   TextWrapping="Wrap"
                                                   Margin="0,4,0,0" />
                                        <TextBlock Text="{Binding CreatedAt, StringFormat=\{0:dd/MM/yyyy HH:mm\}}"
                                                   FontSize="11" Foreground="Silver"
                                                   Margin="0,4,0,0" />
                                    </StackPanel>

                                    <Button Grid.Column="2" Content="Đã đọc"
                                            Style="{StaticResource MaterialDesignFlatButton}"
                                            Command="{Binding DataContext.MarkAsReadCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}"
                                            VerticalAlignment="Center"
                                            FontSize="11" Height="28" />
                                </Grid>
                            </md:Card>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </md:Card>

    </Grid>
</UserControl>
