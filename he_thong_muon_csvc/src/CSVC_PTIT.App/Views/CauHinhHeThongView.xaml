<UserControl x:Class="CSVC_PTIT.App.Views.CauHinhHeThongView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Hướng dẫn -->
        <materialDesign:Card Grid.Row="0" Margin="0,0,0,16" Padding="16" UniformCornerRadius="8">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="InformationOutline" VerticalAlignment="Center"
                                         Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,8,0"/>
                <TextBlock Text="Chỉnh sửa giá trị trực tiếp trong bảng, sau đó nhấn LƯU THAY ĐỔI."
                           VerticalAlignment="Center" Opacity="0.7"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- Loading -->
        <ProgressBar Grid.Row="1"
                     Style="{StaticResource MaterialDesignCircularProgressBar}"
                     IsIndeterminate="True"
                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                     VerticalAlignment="Top" Margin="0,16,0,0"/>

        <!-- DataGrid — inline editing -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Configs}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  materialDesign:DataGridAssist.CornerRadius="8"
                  materialDesign:DataGridAssist.CellPadding="13 8 8 8"
                  HeadersVisibility="Column">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Tham số" Binding="{Binding ConfigKey}" IsReadOnly="True" Width="200"/>
                <DataGridTextColumn Header="Giá trị" Binding="{Binding ConfigValue, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                <DataGridTextColumn Header="Mô tả" Binding="{Binding Description}" IsReadOnly="True" Width="*"/>

                <DataGridTemplateColumn Header="Trạng thái" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ToggleButton Style="{StaticResource MaterialDesignSwitchToggleButton}"
                                          IsChecked="{Binding IsActive, UpdateSourceTrigger=PropertyChanged}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer: Nút Lưu + Thông báo -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center"
                       Foreground="#2E7D32" Margin="0,0,16,0"/>
            <Button Content="LƯU THAY ĐỔI"
                    Command="{Binding SaveAllCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ContentSave" VerticalAlignment="Center"/>
                            <TextBlock Text=" LƯU THAY ĐỔI" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
